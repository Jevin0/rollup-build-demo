export interface ButtonProps {
  /**
   * 按钮类型
   */
  type?: 'primary' | 'default' | 'dashed' | 'danger' | 'link';
  /**
   * 按钮大小
   */
  size?: 'large' | 'default' | 'small';
  /**
   * 按钮形状
   */
}